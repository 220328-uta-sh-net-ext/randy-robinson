using System;
using Classes;


namespace Classes
{
    public class Palindrone
    {
        /* get str = str.toLowerCase().replace(/[\W_]/g, '')
           var str = str.toLowerCase().replace(/[\W_]/g, '')
        **/  
    }
}